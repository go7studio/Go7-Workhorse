{
  "schemaVersion": 1,
  "suite": "go7-workhorse",
  "mode": "baseline-only",
  "source": {
    "channel": "source",
    "repository": "https://github.com/Spikey222/Go7-Workhorse.git",
    "ref": "origin/main",
    "expectedVersion": "0.1.4"
  },
  "install": {
    "existingAppPath": "",
    "packagedAppPath": "",
    "freshUserDataDirectory": "",
    "preserveExistingUserData": true
  },
  "profiles": {
    "grok-acp": {
      "enabled": false,
      "notes": "Detection-only. Do not send model prompts from this eval variant."
    },
    "codex-acp": {
      "enabled": false,
      "notes": "Detection-only. Do not send model prompts from this eval variant."
    },
    "claude-acp": {
      "enabled": false,
      "notes": "Detection-only. Do not send model prompts from this eval variant."
    },
    "custom-openai": {
      "enabled": true,
      "baseUrlEnv": "WORKHORSE_EVAL_MINIMAX_BASE_URL",
      "apiKeyEnv": "WORKHORSE_EVAL_MINIMAX_API_KEY",
      "model": "MiniMax-M3",
      "notes": "The only live model-call profile allowed by this variant. Use MiniMax's OpenAI-compatible endpoint, never Anthropic's API."
    },
    "custom-anthropic": {
      "enabled": false,
      "notes": "Contract/static coverage only. Live Anthropic API calls are prohibited in this variant."
    }
  },
  "modelPolicy": {
    "enforce": true,
    "allowedLiveProfiles": ["custom-openai"],
    "allowedModels": ["MiniMax-M3"],
    "forbidAnthropicApi": true,
    "notes": "Harness discovery may inspect redacted local readiness, but every model prompt must run as MiniMax-M3."
  },
  "spend": {
    "liveApiAllowed": false,
    "approvedUsd": 0,
    "approvalTicket": ""
  },
  "safety": {
    "allowNetwork": false,
    "allowOutsideWorkspace": false,
    "allowDestructiveProductActions": false,
    "fixtureWorkspaceOnly": true
  },
  "evidence": {
    "screenshots": true,
    "screenRecording": false,
    "supportReport": true,
    "redactSecrets": true
  }
}
